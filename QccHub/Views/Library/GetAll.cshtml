@model IEnumerable<LibraryItem>
@{
    ViewBag.Title = "Library";
}
@foreach (var item in Model)
{
    <h2>@item.Title</h2>
    <h2>@item.Details</h2>
    <a href="@Url.Content("~/LibraryFiles")/@item.File"></a>
    <img src="@Url.Content("~/LibraryImages")/@item.Image"/>
    <hr>
}